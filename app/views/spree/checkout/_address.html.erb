<div class='col-md-12'>
  <div class="panel panel-info" data-hook="shipping_fieldset_wrapper">
    <div class="panel-heading">
      <%= Spree.t(:Shipping_Address) %>
    </div>
      <%= form.fields_for :ship_address do |ship_form| %>
        <%= render :partial => 'spree/address/form', :locals => { :form => ship_form, :address_type => 'shipping', :address => @order.ship_address } %>
      <% end %>
  </div>
  <p class="field checkbox" data-hook="use_billing">
    <%= check_box_tag 'order[use_billing]', '1', @order.shipping_eq_billing_address? %>
    <%= label_tag :order_use_billing, Spree.t(:use_billing_address), id: 'use_billing' %>
  </p>
  <div class="panel panel-info" id="billingstyle" data-hook="billing_fieldset_wrapper">
    <div class="panel-heading">
      <%= Spree.t(:Billing_Address) %>
    </div>
    <%= form.fields_for :bill_address do |bill_form| %>
      <%= render :partial => 'spree/address/form', :locals => { :form => bill_form, :address_type => 'billing', :address => @order.bill_address } %>
    <% end %>
  </div>
</div>

<div class="form-buttons col-md-12 text-center" data-hook="buttons">
  <%= submit_tag Spree.t(:save_and_continue), :class => 'continue btn btn-default' %>
</div>
